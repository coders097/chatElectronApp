{
  "name": "simplechatapp",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "start tsc -w && start node-sass --watch scss -o css",
    "startApp":"electron ."
  },
  "author": "BMD",
  "license": "MIT",
  "devDependencies": {
    "electron": "^16.0.4"
  },
  "dependencies": {
    "node-sass": "^7.0.0",
    "typescript": "^4.5.2"
  }
}

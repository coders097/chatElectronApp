{
  "name": "simplechatapp",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "start tsc -w && start node-sass --watch scss -o css",
    "startApp": "electron ."
  },
  "author": "BMD",
  "license": "MIT",
  "devDependencies": {
    "electron": "^16.0.4"
  },
  "dependencies": {
    "@electron/remote": "^2.0.1",
    "electron-prompt": "^1.7.0",
    "node-sass": "^7.0.0",
    "socket.io-client": "^4.4.0",
    "typescript": "^4.5.2"
  }
}
